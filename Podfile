# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'RecipeBrowser' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for RecipeBrowser
  pod 'moa', '~> 12.0'

  target 'RecipeBrowserTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'RecipeBrowserUITests' do
    # Pods for testing
  end

end
